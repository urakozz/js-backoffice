/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LinklyPipe } from './linkly.pipe';

describe('Pipe: Linkly', () => {
  it('create an instance', () => {
    let pipe = new LinklyPipe();
    expect(pipe).toBeTruthy();
  });
});
